name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      # 1. سحب الكود
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. تثبيت بايثون
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. تثبيت Buildozer والمكتبات (تم إضافة automake و libtool-bin و python-for-android)
      - name: Install Buildozer and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git python3 python3-dev openjdk-17-jdk unzip
          # إضافة automake و libtool-bin لحل مشكلة undefined macro
          sudo apt-get install -y autoconf libtool libtool-bin automake pkg-config libncurses-dev libssl-dev libffi-dev libgdbm-dev zlib1g-dev
          # تحديث صريح لـ python-for-android لضمان التوافق مع Ubuntu 24.04
          pip install --upgrade buildozer cython python-for-android

      # 4. إعداد Android SDK
      - name: Setup Android SDK
        run: |
          SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk
          mkdir -p $SDK_ROOT/cmdline-tools/latest
          
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O /tmp/cmdline-tools.zip
          unzip -q /tmp/cmdline-tools.zip -d $SDK_ROOT/cmdline-tools/
          
          mv $SDK_ROOT/cmdline-tools/cmdline-tools/* $SDK_ROOT/cmdline-tools/latest/
          
          # الرابط الوهمي القسري
          ln -sf $SDK_ROOT/cmdline-tools/latest $SDK_ROOT/tools
          
          chmod +x $SDK_ROOT/cmdline-tools/latest/bin/*
          
          echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK_ROOT" >> $GITHUB_ENV
          echo "$SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$SDK_ROOT/tools/bin" >> $GITHUB_PATH

      # 5. قبول التراخيص
      - name: Accept Android SDK Licenses
        run: yes | sdkmanager --licenses

      # 6. التخزين المؤقت
      - name: Cache Buildozer
        uses: actions/cache@v3
        with:
          path: |
            ~/.buildozer
            buildozer.spec
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      # 7. تنفيذ أمر البناء
      - name: Build APK
        run: buildozer android debug

      # 8. رفع ملف الـ APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: my-app-apk
          path: bin/*.apk
